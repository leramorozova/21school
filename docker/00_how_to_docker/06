docker run -d --name overlord --restart=always -p 5000:80 nginx
# to connect http://192.168.99.100:5000/
# to see: docker ps -a
# to remove: sudo docker rm overlord
# to stop: sudo docker stop overlord
